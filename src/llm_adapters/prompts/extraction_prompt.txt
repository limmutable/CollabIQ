TASK: Extract structured data from email in strict JSON format.

REQUIRED OUTPUT SCHEMA:
{
    "person_in_charge": {"value": string | null, "confidence": float (0.0-1.0)},
    "startup_name": {"value": string | null, "confidence": float (0.0-1.0)},
    "partner_org": {"value": string | null, "confidence": float (0.0-1.0)},
    "details": {"value": string (required, non-empty), "confidence": float (0.0-1.0)},
    "date": {"value": string (YYYY-MM-DD format) | null, "confidence": float (0.0-1.0)}
}

EXTRACTION RULES:
1. person_in_charge: Full name of person mentioned (null if none)
2. startup_name: Startup/company name (null if none)
3. partner_org: Partner organization name (null if none)
4. details: Main content/topic (ALWAYS extract, never null)
5. date: Parse to YYYY-MM-DD (null if no date found)

CONFIDENCE GUIDELINES:
- Explicit mention: 0.9-1.0
- Clear from context: 0.7-0.9
- Inferred/guessed: 0.5-0.7
- Not found: 0.0

VALIDATION:
- ALL fields must be present
- Each field must have both "value" and "confidence"
- Confidence values must be 0.0-1.0
- Date must be valid YYYY-MM-DD or null
- Output must be valid JSON

# Now extract from the following email:
