"""
Integration tests for CLI email command workflows.

Tests the interaction between email commands (fetch → clean → list → process).
"""

import pytest
from typer.testing import CliRunner

# Tests will be added in Phase 4 (User Story 2)
